#include "main.h"

/**
 * mul - multiplies integers
 * @a: first parameter
 * @b: substitute for second variable
 * Return: result of multiplication
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
